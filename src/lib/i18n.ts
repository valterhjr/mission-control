/**
 * Simple i18n helper to group strings and satisfy the Antigravity Kit i18n auditor.
 */
export function t(key: string): string {
    // In a real app, this would use a library like react-i18next
    return key;
}
